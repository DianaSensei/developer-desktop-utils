{
  "name": "devtool-mcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "MCP stdio server that lets an MCP client (Claude Desktop/Code) inspect and drive DevTool's API Client — collections, environments, requests, scripts — while the desktop app is open.",
  "type": "module",
  "bin": {
    "devtool-mcp-server": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.0"
  }
}
