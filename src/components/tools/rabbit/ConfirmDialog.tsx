// Re-exported from the shared foundation (src/components/ui/confirm-dialog.tsx)
// so existing imports of './ConfirmDialog' inside the rabbit/ tool keep working
// without every call site needing to change its import path.
export { ConfirmDialog, type ConfirmDialogProps } from '@/components/ui/confirm-dialog';
