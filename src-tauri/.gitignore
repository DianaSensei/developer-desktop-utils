# Generated by Cargo
target/

# Generated by Tauri at build time — platform-specific (desktop/macOS/windows/
# linux schemas, acl-manifests, merged capabilities). These are derived from
# src-tauri/capabilities/*.json, so committing them causes cross-platform churn
# (e.g. a stray linux-schema.json appears when building on Ubuntu).
gen/schemas/
