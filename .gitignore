# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target

# MCP sidecar binaries — built per-platform by scripts/prepare-mcp-sidecar.mjs
# right before `tauri build` (see package.json's tauri:build), not committed.
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep

# Coverage reports
lcov.info
**/lcov.info
/coverage